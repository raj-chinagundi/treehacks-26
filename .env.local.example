# ── NextAuth ──────────────────────────────────────────────────────────────────
# Required in production; a fallback dev value is baked in for local runs.
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=change-me-in-production

# ── Google OAuth (optional) ───────────────────────────────────────────────────
# Leave these commented out to use the built-in mock/demo sign-in instead.
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# ── Gemini API (optional — entered by the user in the chatbot UI) ──────────────
# The Gemini key is entered by the user in the chat panel and stored in
# sessionStorage only. It is NOT read from this file at runtime.
# GEMINI_API_KEY=not-used-server-side
